---
import { ArrowDown, ArrowRight, ArrowUpRight, Cloud, Cpu, Database, FolderOpen, Globe, HardDrive, KeyRound, Monitor, Radio, Server, ShieldCheck, Terminal } from '@lucide/astro';
import SiteLayout from '../layouts/SiteLayout.astro';

const title = 'Architecture | Eugene Plexus';
const description = 'How the parts of Eugene Plexus fit together: the agent on each machine, the gateway your tools talk to, the drivers and engines behind it, the model library, and the control root that ties several machines into one install.';

const components = [
  {
    id: 'agent', name: 'Agent', icon: Server, where: 'One per machine',
    what: 'The process on each machine. It installs and updates llama.cpp, supervises components and engines, checks memory before launch, serves the web UI, and optionally keeps local copies of the models this node runs.',
    talks: 'Local components and engines, the control root for enrollment and discovery, and other agents for console requests.', repo: 'agent',
  },
  {
    id: 'gateway', name: 'Gateway', icon: Radio, where: 'One per install',
    what: 'One address serving OpenAI-compatible chat and embeddings plus Anthropic Messages. Both chat formats share routing, replica balancing, ordered failover, wake-on-demand, and retained request metrics.',
    talks: 'Your tools on one side; every inference driver on the other. Reads model defaults from the Library through its agent.', repo: 'gateway',
  },
  {
    id: 'driver', name: 'Inference driver', icon: Cpu, where: 'One per backend',
    what: 'A small adapter in front of one backend: a llama.cpp or vLLM runtime, an Ollama or LM Studio server, a cloud API, or a subscription CLI. It translates requests and reports what the backend can do.',
    talks: 'The gateway above it; one backend below it.', repo: 'inference-driver',
  },
  {
    id: 'library', name: 'Library', icon: Database, where: 'One per install',
    what: 'Your model folders, with GGUF and safetensors scanning, named profiles for launch and generation settings, catalogue search and resumable downloads. It suggests a starter model and estimates memory fit at a stated context length for the selected machine.',
    talks: 'The disk, the model hub, agents launching models, and gateways reading model defaults.', repo: 'library',
  },
  {
    id: 'control', name: 'Control root', icon: ShieldCheck, where: 'One active per install',
    what: 'The trust root, on a single machine or across a homelab. It maintains the node registry and replicated control state, issues one-time join tokens, coordinates signed rekeying, and can have warm standbys.',
    talks: 'Every machine’s agent. It spawns nothing itself.', repo: 'control',
  },
  {
    id: 'ui', name: 'Web UI', icon: Monitor, where: 'Served by every agent',
    what: 'Home for setup and a first reply; a resource tree for the whole install. Includes model discovery, downloads and loading in a tasks tray, actionable issues, remote folder browsing, client keys, metrics, and a diagnostic playground. Static files, not a separate server.',
    talks: 'Its own agent, which forwards to the right component wherever it runs.', repo: 'ui',
  },
];

const journey = [
  { step: '1', title: 'Your tool asks', text: 'An editor or coding client sends OpenAI-style chat or Anthropic Messages with a client key. The Messages route accepts a bearer token or x-api-key; OpenAI routes use bearer tokens.' },
  { step: '2', title: 'The gateway chooses', text: 'It resolves the model, checks readiness, wakes it if needed, and balances replicas. Omitted generation settings come from that model’s default profile, then gateway settings. Eligible backend failures can move the request to the next configured tier.' },
  { step: '3', title: 'The driver translates', text: 'One driver turns the request into its backend protocol and reports capabilities. Token streaming depends on that backend; Codex CLI returns whole messages rather than individual tokens.' },
  { step: '4', title: 'The engine generates', text: 'llama.cpp, vLLM, Ollama, or a cloud provider does the actual work on the hardware it has.' },
  { step: '5', title: 'The request is recorded', text: 'Metrics retain the serving backend, timing, tier, and failed attempts for both chat formats. OpenAI replies include routing metadata in the body or final stream frame; Anthropic keeps its own body format and uses headers.' },
];

const glossary = [
  { term: 'Install', text: 'One control plane: a control root, a gateway, a library, and every machine that has joined them. One passphrase.' },
  { term: 'Node', text: 'A machine in an install. Each node runs one agent.' },
  { term: 'Component', text: 'One of the four services: gateway, inference driver, library, control root. The agent starts them.' },
  { term: 'Runtime', text: 'An inference engine the agent started and supervises, such as one llama.cpp process serving one model.' },
  { term: 'Backend', text: 'Anything a driver can send requests to: a runtime, an external server like Ollama, a cloud API, or a subscription CLI.' },
  { term: 'Replica', text: 'Two or more backends serving the same model. The gateway balances between them.' },
];
---

<SiteLayout title={title} description={description}>
  <section class="section arch-hero" aria-labelledby="arch-title">
    <div class="container">
      <p class="eyebrow"><span class="section-index">Architecture</span></p>
      <h1 id="arch-title">How it fits together<span class="heading-period">.</span></h1>
      <p class="arch-lead">Eugene Plexus installs and manages engines, helps choose model settings for your hardware, and connects compatible tools to local and cloud backends. One machine or a homelab, the same layers do the work. It is not an inference engine or a chat app.</p>
      <p class="arch-lead">Development snapshot: <time datetime="2026-09-20">September 20, 2026</time>. <a href="/install">First alpha available for testing</a>. <a href="#status">Current boundaries</a>.</p>
    </div>
  </section>

  <section class="section arch-section arch-layers-section" id="layers" aria-labelledby="layers-title">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow"><span class="section-index">01 /</span> The layers</p>
        <h2 id="layers-title">Five layers, one address.</h2>
        <p>Inference flows through the gateway and a driver. Beside that path, the agent supervises engines, the library prepares models, and the control root maintains the install. Discovery, key checks, model defaults and a model wake can still depend on those services.</p>
      </div>

      <figure class="layers" aria-labelledby="layers-caption">
        <div class="layers-stack">
          <div class="layer layer-tools">
            <div class="layer-label"><Terminal size={18} aria-hidden="true" /><span>Your tools</span></div>
            <ul class="chips">
              <li>Claude Code</li><li>OpenAI-compatible tools</li><li>Editors</li><li>The playground</li>
            </ul>
            <p class="layer-note">Supported chat, tool-call and embedding requests, with revocable client keys. <a href="#status">Compatibility has limits.</a></p>
          </div>
          <div class="layer-arrow" aria-hidden="true"><ArrowDown size={18} /><span>OpenAI or Anthropic Messages</span><ArrowDown size={18} /></div>
          <div class="layer layer-gateway">
            <div class="layer-label"><Radio size={18} aria-hidden="true" /><span>Gateway</span></div>
            <ul class="chips">
              <li>Routing</li><li>Replica balancing</li><li>Failover tiers</li><li>Idle unload &amp; wake</li><li>Metrics</li>
            </ul>
            <p class="layer-note">One per install. The only thing your tools need to know about.</p>
          </div>
          <div class="layer-arrow" aria-hidden="true"><ArrowDown size={18} /><span>one driver per backend</span><ArrowDown size={18} /></div>
          <div class="layer layer-drivers">
            <div class="layer-label"><Cpu size={18} aria-hidden="true" /><span>Inference drivers</span></div>
            <ul class="chips">
              <li>Driver</li><li>Driver</li><li>Driver</li><li>Driver</li>
            </ul>
            <p class="layer-note">A thin adapter in front of each backend. It translates and reports capabilities.</p>
          </div>
          <div class="layer-arrow" aria-hidden="true"><ArrowDown size={18} /><span>the backend&rsquo;s own API</span><ArrowDown size={18} /></div>
          <div class="layer layer-engines">
            <div class="layer-label"><Cloud size={18} aria-hidden="true" /><span>Engines and backends</span></div>
            <ul class="chips">
              <li class="chip-strong">llama.cpp</li><li class="chip-strong">vLLM</li><li>Ollama</li><li>LM Studio</li><li>Cloud APIs</li><li>Claude Code CLI</li><li>Codex CLI</li>
            </ul>
            <p class="layer-note">llama.cpp is installed and supervised; vLLM is user-installed and supervised. External servers stay under your control. CLI backends invoke your installed tools using their existing login.</p>
          </div>
          <div class="layer-arrow" aria-hidden="true"><ArrowDown size={18} /><span>runs on</span><ArrowDown size={18} /></div>
          <div class="layer layer-hardware">
            <div class="layer-label"><HardDrive size={18} aria-hidden="true" /><span>Your hardware</span></div>
            <ul class="chips">
              <li>GPUs</li><li>RAM</li><li>Your model directories</li>
            </ul>
            <p class="layer-note">Local engines use your machines. Cloud providers run on their own hardware.</p>
          </div>
        </div>

        <aside class="layers-rail" aria-label="Services beside the request path">
          <div class="rail-card rail-agent">
            <div class="layer-label"><Server size={18} aria-hidden="true" /><span>Agent</span></div>
            <p>One per machine. Starts and watches everything on this box. Serves the web UI.</p>
          </div>
          <div class="rail-card rail-library">
            <div class="layer-label"><Database size={18} aria-hidden="true" /><span>Library</span></div>
            <p>Your models on disk, per-model settings, discovery, downloads, and fit guidance.</p>
          </div>
          <div class="rail-card rail-control">
            <div class="layer-label"><ShieldCheck size={18} aria-hidden="true" /><span>Control root</span></div>
            <p>Which machines belong to this install, and the keys they trust each other with.</p>
          </div>
        </aside>
        <figcaption id="layers-caption">Client inference goes directly to the gateway. The browser console uses the agent as a proxy; the control root does not relay model output.</figcaption>
      </figure>
    </div>
  </section>

  <section class="section arch-section" id="components" aria-labelledby="components-title">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow"><span class="section-index">02 /</span> The components</p>
        <h2 id="components-title">Six building blocks, distinct jobs.</h2>
        <p>Each has its own repository. The four service kinds are gateway, inference driver, library and control root; the agent supervises them, and the UI is a static export it serves. Six consumers generate types from pinned OpenAPI 3.1 contracts in the specs repository. Components share schemas, not code.</p>
      </div>
      <div class="component-grid">
        {components.map((c) => (
          <article class="component-card" id={c.id}>
            <div class="component-head">
              <c.icon size={24} stroke-width={1.5} aria-hidden="true" />
              <h3>{c.name}</h3>
              <span class="where">{c.where}</span>
            </div>
            <p>{c.what}</p>
            <p class="talks"><strong>Talks to:</strong> {c.talks}</p>
            <a href={`https://github.com/eugene-plexus/${c.repo}`}>Source <ArrowUpRight size={14} aria-hidden="true" /></a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="section arch-section arch-journey-section" id="request" aria-labelledby="request-title">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow"><span class="section-index">03 /</span> One request</p>
        <h2 id="request-title">What happens when a tool asks a question.</h2>
        <p>The whole point of the layering: your tool never has to know which machine, which engine, or which model file. It asks the gateway, and the answer says where it went.</p>
      </div>
      <ol class="journey">
        {journey.map((j, i) => (
          <li class="journey-step">
            <span class="journey-number" aria-hidden="true">{j.step}</span>
            <h3>{j.title}</h3>
            <p>{j.text}</p>
            {i < journey.length - 1 && <ArrowRight class="journey-arrow" size={20} aria-hidden="true" />}
          </li>
        ))}
      </ol>
      <p class="journey-rule"><strong>One rule worth knowing:</strong> failover happens before the first token. Once an answer has started streaming, the gateway will not splice a second model onto it. A cut-off answer is reported as cut off, never quietly completed by someone else.</p>
      <p class="journey-rule"><strong>Not every failure is retryable:</strong> a deadline while a backend is still computing returns a timeout, not a second computation on another replica. An unreachable host can still fail over. Embeddings fail over only between replicas of the same model, never into a different vector space.</p>
    </div>
  </section>

  <section class="section arch-section" id="topologies" aria-labelledby="topologies-title">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow"><span class="section-index">04 /</span> One computer, or several</p>
        <h2 id="topologies-title">The same pieces, placed differently.</h2>
        <p>There is one shape, not two products. On a single PC every component runs beside the engines. Across machines, the control plane moves to wherever is convenient and each GPU box keeps only what needs the hardware.</p>
      </div>
      <div class="topologies">
        <figure class="topology">
          <figcaption><Monitor size={18} aria-hidden="true" /> One computer</figcaption>
          <div class="machine">
            <div class="machine-title">Your PC</div>
            <div class="box box-agent">Agent</div>
            <div class="box-row">
              <div class="box">Control root</div>
              <div class="box">Gateway</div>
              <div class="box">Library</div>
            </div>
            <div class="box-row">
              <div class="box box-driver">Driver</div>
              <div class="box box-driver">Driver</div>
            </div>
            <div class="box-row">
              <div class="box box-engine">llama.cpp</div>
              <div class="box box-engine">Ollama</div>
            </div>
            <div class="box box-disk"><FolderOpen size={15} aria-hidden="true" /> Your model folders</div>
          </div>
          <p class="topology-note">Everything on one box. The browser opens the agent&rsquo;s address and the gateway&rsquo;s port is the one your tools use.</p>
        </figure>

        <figure class="topology topology-multi">
          <figcaption><Globe size={18} aria-hidden="true" /> Several machines</figcaption>
          <div class="multi">
            <div class="machine machine-plane">
              <div class="machine-title">Control-plane host <span>a NAS container, a small server, or one of the PCs</span></div>
              <div class="box box-agent">Agent</div>
              <div class="box-row">
                <div class="box">Control root</div>
                <div class="box">Gateway</div>
                <div class="box">Library</div>
              </div>
              <div class="box box-disk"><FolderOpen size={15} aria-hidden="true" /> Shared model folder</div>
            </div>
            <div class="multi-links" aria-hidden="true">
              <span class="link-line"></span>
              <span class="link-label">joined to one install &middot; one passphrase &middot; models mounted on each GPU box</span>
              <span class="link-line"></span>
            </div>
            <div class="gpu-boxes">
              <div class="machine machine-gpu">
                <div class="machine-title">GPU machine A</div>
                <div class="box box-agent">Agent</div>
                <div class="box-row"><div class="box box-driver">Driver</div><div class="box box-driver">Driver</div></div>
                <div class="box-row"><div class="box box-engine">vLLM</div><div class="box box-engine">llama.cpp</div></div>
              </div>
              <div class="machine machine-gpu">
                <div class="machine-title">GPU machine B</div>
                <div class="box box-agent">Agent</div>
                <div class="box-row"><div class="box box-driver">Driver</div></div>
                <div class="box-row"><div class="box box-engine">Ollama</div></div>
              </div>
            </div>
          </div>
          <p class="topology-note">Each machine runs an agent. A Library folder records its mounts once; nodes can override the path and optionally keep a local copy. Tools use one gateway, and any agent&rsquo;s console can manage the enrolled nodes.</p>
        </figure>
      </div>
    </div>
  </section>

  <section class="section arch-section arch-principles-section" id="principles" aria-labelledby="principles-title">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow"><span class="section-index">05 /</span> What stays where</p>
        <h2 id="principles-title">Yours, on your disk, in plain sight.</h2>
      </div>
      <div class="principles">
        <article class="principle">
          <FolderOpen size={27} stroke-width={1.5} aria-hidden="true" />
          <h3>Model files</h3>
          <p>In the directories you point the library at, with plain names. Downloads land there too. No content-addressed cache, no hidden store. Remove Eugene Plexus and the files are still there.</p>
          <span class="principle-note">Originals stay yours; local copies are optional.</span>
        </article>
        <article class="principle">
          <KeyRound size={27} stroke-width={1.5} aria-hidden="true" />
          <h3>Secrets and trust</h3>
          <p>Operator sessions administer the install; named client keys allow inference and can be revoked. Nodes enroll with one-time join tokens, then sign address announcements and verify signed rekeys. With Ed25519 signing, agents and control roots hold signing authority; gateways, libraries and drivers receive public verification keys.</p>
          <span class="principle-note">Trusted hosts and a LAN or mesh VPN, not public exposure.</span>
        </article>
        <article class="principle">
          <Cpu size={27} stroke-width={1.5} aria-hidden="true" />
          <h3>Engines</h3>
          <p>llama.cpp is fetched, verified and kept in versioned directories by the agent. vLLM is one you install; the agent finds and drives it. Neither is forked or replaced.</p>
          <span class="principle-note">Upstream, supervised.</span>
        </article>
      </div>
      <p class="journey-rule"><strong>Optional local copies:</strong> a node copies only model files its own declared runtimes use, into a separate directory with a minimum-free-space setting. It manages those copies, not the source files in your Library folders. The first copy takes time; later starts can avoid reading the model over the network again.</p>
    </div>
  </section>

  <section class="section arch-section" id="status" aria-labelledby="status-title">
    <div class="container glossary-layout">
      <div>
        <p class="eyebrow"><span class="section-index">06 /</span> Current boundaries</p>
        <h2 id="status-title">An alpha, with limits to know.</h2>
        <p class="glossary-intro">As of September 20, 2026. The first alpha is available for early testing. There is no stable release yet. Versioned installers and a matching container image are available; this is not a production-readiness claim. <a href="/install">Installation guide</a>.</p>
        <p class="glossary-intro">The <a href="https://github.com/eugene-plexus/specs/tree/main/docs/acceptance">acceptance records</a> document the test setups, observed behavior and limits of what has been verified.</p>
        <p class="glossary-intro">For timings, see the <a href="https://github.com/eugene-plexus/specs/blob/main/docs/acceptance/control-plane-measurements.md">control-plane measurement record</a>, with hardware, baselines and limits for each observation.</p>
        <a class="text-link" href="https://github.com/eugene-plexus/specs/blob/main/docs/design/release-roadmap.md">Follow the roadmap <ArrowUpRight size={17} aria-hidden="true" /></a>
      </div>
      <dl class="glossary">
        <div class="glossary-row">
          <dt>Client APIs</dt>
          <dd>OpenAI-compatible chat, tool calling, streaming and embeddings; Anthropic Messages for text and tool calls. Both chat formats use the same routing and metrics. A <a href="https://github.com/eugene-plexus/specs/blob/main/docs/acceptance/anthropic-messages-run.md">real Claude Code tool loop</a> passed through the gateway with a stub driver, not a real model. This is not full vendor API parity.</dd>
        </div>
        <div class="glossary-row">
          <dt>Messages limits</dt>
          <dd>Image and document blocks, server-side tools and MCP servers are rejected. Anthropic <code>thinking</code>, <code>cache_control</code> and <code>top_k</code> settings are ignored; local thinking behavior follows the configured model settings. See the <a href="https://github.com/eugene-plexus/specs/blob/main/openapi/gateway.yaml">gateway contract</a> for the complete mapping.</dd>
        </div>
        <div class="glossary-row">
          <dt>Engines and hosts</dt>
          <dd>llama.cpp and user-installed vLLM have live acceptance records on NVIDIA hardware. Windows and Linux via WSL2 have been exercised. AMD and Intel detection fixes have tests, but not equivalent live-hardware coverage. macOS remains unverified on hardware; MLX is experimental on a separate branch, not main.</dd>
        </div>
        <div class="glossary-row">
          <dt>Windows startup</dt>
          <dd>The Windows installer runs a real service, with explicit migration from older per-user installs. Starting and stopping a LocalSystem service is exercised in Windows CI; reboot-before-sign-in verification remains outstanding. See the <a href="https://github.com/eugene-plexus/specs/blob/main/docs/design/release-roadmap.md">verification work still on the roadmap</a>.</dd>
        </div>
        <div class="glossary-row">
          <dt>Trust boundary</dt>
          <dd>New installs use Ed25519 signing with public-only verification in gateway, Library and driver processes. Older installs retain legacy signing until explicitly rotated after all components are upgraded. <a href="https://github.com/eugene-plexus/specs/blob/main/docs/acceptance/r7-signing-run.md">Windows and Linux rotation checks</a> preserve enrollment and reject forged sessions. Trusted agents remain able to sign tokens.</dd>
        </div>
        <div class="glossary-row">
          <dt>Model settings</dt>
          <dd>The default profile supplies maximum output tokens, temperature and top-p when your app omits them. Explicit app settings win. Changes apply after cache refresh without restarting the model; bounded caching lets requests continue during Library outages. <a href="https://github.com/eugene-plexus/specs/blob/main/docs/acceptance/r8-profile-defaults-run.md">Both API formats and streaming are checked</a>. Launch settings still apply when a runtime is created.</dd>
        </div>
        <div class="glossary-row">
          <dt>Still to build</dt>
          <dd>Phone, accessibility and moderated usability work remains. Saved llama.cpp profiles now have a Benchmark button to measure decode speed at increasing context depths on a selected machine. Fit guidance still estimates memory use; benchmark results describe that particular run.</dd>
        </div>
      </dl>
    </div>
  </section>

  <section class="section arch-section arch-glossary-section" id="glossary" aria-labelledby="glossary-title">
    <div class="container glossary-layout">
      <div>
        <p class="eyebrow"><span class="section-index">07 /</span> The words</p>
        <h2 id="glossary-title">A short glossary.</h2>
        <p class="glossary-intro">Six words that mean specific things here.</p>
        <a class="text-link" href="https://github.com/eugene-plexus/specs/tree/main/docs/design">Read the design documents <ArrowUpRight size={17} aria-hidden="true" /></a>
      </div>
      <dl class="glossary">
        {glossary.map((g) => (
          <div class="glossary-row">
            <dt>{g.term}</dt>
            <dd>{g.text}</dd>
          </div>
        ))}
      </dl>
    </div>
  </section>
</SiteLayout>

<style>
  .arch-hero {
    padding-block: 72px 32px;
  }
  .arch-hero h1 {
    font-size: 44px;
    line-height: 1.1;
    letter-spacing: 0;
    margin-top: 18px;
  }
  .arch-lead {
    color: var(--muted);
    font-size: 17px;
    max-width: 700px;
    margin-top: 22px;
  }
  .arch-section {
    padding-block: 72px;
    border-top: 1px solid var(--border);
  }
  .arch-layers-section,
  .arch-journey-section {
    background: var(--panel-soft);
  }

  /* --- layers ------------------------------------------------------- */
  .layers {
    margin: 48px 0 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr) 260px;
    gap: 28px;
    align-items: start;
  }
  .layers-stack {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .layer {
    background: var(--panel);
    border: 1px solid var(--border);
    border-left: 4px solid var(--accent-left);
    border-radius: var(--radius);
    padding: 16px 18px;
  }
  .layer-gateway { border-left-color: var(--accent-right); }
  .layer-engines { border-left-color: #2f9e6e; }
  .layer-hardware { border-left-color: #8a8a93; }
  .layer-label {
    display: flex;
    align-items: center;
    gap: 9px;
    font-weight: 600;
    font-size: 15px;
  }
  .layer-label svg { color: var(--accent-left); }
  .layer-gateway .layer-label svg { color: var(--accent-right); }
  .layer-engines .layer-label svg { color: #2f9e6e; }
  .layer-hardware .layer-label svg { color: #8a8a93; }
  .chips {
    list-style: none;
    margin: 12px 0 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .chips li {
    font-family: var(--font-mono);
    font-size: 12px;
    padding: 4px 10px;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: var(--panel-soft);
    color: var(--foreground);
  }
  .chips .chip-strong {
    border-color: #2f9e6e;
    font-weight: 600;
  }
  .layer-note {
    color: var(--muted);
    font-size: 13px;
    margin-top: 10px;
  }
  .layer-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 11px;
    padding: 2px 0;
  }
  .layers-rail {
    display: flex;
    flex-direction: column;
    gap: 12px;
    position: sticky;
    top: 24px;
  }
  .rail-card {
    background: var(--panel);
    border: 1px dashed var(--border-hover);
    border-radius: var(--radius);
    padding: 14px 16px;
  }
  .rail-card p {
    color: var(--muted);
    font-size: 13px;
    margin-top: 8px;
  }
  .rail-control .layer-label svg { color: var(--accent-right); }
  .layers figcaption {
    grid-column: 1 / -1;
    color: var(--muted);
    font-size: 13px;
    margin-top: 6px;
  }

  /* --- components --------------------------------------------------- */
  .component-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
    margin-top: 48px;
  }
  .component-card {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 22px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .component-head {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }
  .component-head svg { color: var(--accent-left); }
  .component-card:nth-child(2) .component-head svg,
  .component-card:nth-child(5) .component-head svg { color: var(--accent-right); }
  .component-head h3 {
    font-size: 18px;
  }
  .where {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 2px 9px;
    white-space: nowrap;
  }
  .component-card > p {
    color: var(--muted);
    font-size: 14px;
  }
  .component-card .talks {
    color: var(--foreground);
    font-size: 13px;
  }
  .component-card > a {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-size: 13px;
    font-weight: 500;
    text-decoration: none;
  }
  .component-card > a:hover { text-decoration: underline; }

  /* --- journey ------------------------------------------------------ */
  .journey {
    list-style: none;
    margin: 48px 0 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 18px;
  }
  .journey-step {
    position: relative;
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 20px 18px;
  }
  .journey-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 999px;
    background: var(--accent-left);
    color: var(--on-accent-left);
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 600;
  }
  .journey-step:nth-child(4) .journey-number { background: #2f9e6e; }
  .journey-step:nth-child(5) .journey-number { background: var(--accent-right); }
  .journey-step h3 {
    font-size: 15px;
    margin-top: 14px;
  }
  .journey-step p {
    color: var(--muted);
    font-size: 13px;
    margin-top: 8px;
  }
  /* Inside the card's box, not hanging over its edge: an element that
     pokes outside its parent registers as overflow, and the test that
     guards against horizontal scrolling reads it as such. */
  .journey-arrow {
    position: absolute;
    right: 12px;
    top: 24px;
    color: var(--border-hover);
  }
  .journey-rule {
    margin-top: 28px;
    max-width: 760px;
    font-size: 14px;
    color: var(--muted);
  }
  .journey-rule strong { color: var(--foreground); }

  /* --- topologies --------------------------------------------------- */
  .topologies {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.6fr);
    gap: 28px;
    margin-top: 48px;
  }
  .topology {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .topology figcaption {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
    font-size: 15px;
  }
  .topology figcaption svg { color: var(--accent-left); }
  .topology-multi figcaption svg { color: var(--accent-right); }
  .machine {
    background: var(--panel);
    border: 1px solid var(--border-hover);
    border-radius: var(--radius);
    padding: 14px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .machine-title {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 4px;
  }
  .machine-title span {
    display: block;
    font-weight: 400;
    color: var(--muted);
  }
  .box {
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--panel-soft);
    padding: 7px 10px;
    font-size: 13px;
    text-align: center;
    flex: 1;
    min-width: 0;
  }
  .box-row {
    display: flex;
    gap: 8px;
  }
  .box-agent {
    border-color: var(--accent-left);
    border-style: dashed;
    font-weight: 600;
  }
  .box-driver { background: var(--panel); }
  .box-engine {
    border-color: #2f9e6e;
    font-family: var(--font-mono);
    font-size: 12px;
  }
  .box-disk {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    border-style: dotted;
    color: var(--muted);
  }
  .multi {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .multi-links {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 11px;
    text-align: center;
  }
  .link-line {
    flex: 1;
    height: 0;
    border-top: 1px dashed var(--border-hover);
  }
  .gpu-boxes {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }
  .topology-note {
    color: var(--muted);
    font-size: 13px;
  }

  /* --- principles & glossary ---------------------------------------- */
  .arch-principles-section .principles { margin-top: 44px; }
  .arch-glossary-section { background: var(--panel-soft); }
  .glossary-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.5fr);
    gap: 48px;
    align-items: start;
  }
  .glossary-intro {
    color: var(--muted);
    font-size: 15px;
    margin-top: 22px;
  }
  .glossary-layout .text-link { margin-top: 20px; }
  .glossary {
    margin: 0;
    display: flex;
    flex-direction: column;
  }
  .glossary-row {
    display: grid;
    grid-template-columns: 130px minmax(0, 1fr);
    gap: 18px;
    padding: 14px 0;
    border-top: 1px solid var(--border);
  }
  .glossary-row:last-child { border-bottom: 1px solid var(--border); }
  .glossary dt {
    font-weight: 600;
    font-size: 14px;
  }
  .glossary dd {
    margin: 0;
    color: var(--muted);
    font-size: 14px;
  }
  .glossary code { overflow-wrap: anywhere; }

  @media (max-width: 1050px) {
    .journey { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .journey-arrow { display: none; }
    .component-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .topologies { grid-template-columns: 1fr; }
  }
  @media (max-width: 760px) {
    .arch-hero h1 { font-size: 34px; }
    .arch-section { padding-block: 56px; }
    .layers { grid-template-columns: 1fr; }
    .layers-rail { position: static; }
    .component-grid,
    .journey,
    .gpu-boxes { grid-template-columns: 1fr; }
    .glossary-layout { grid-template-columns: 1fr; gap: 28px; }
    .glossary-row { grid-template-columns: 1fr; gap: 4px; }
    .where { margin-left: 0; }
  }
  @media (max-width: 390px) {
    .box-row { flex-direction: column; }
    .layer-arrow span { display: none; }
  }
</style>
